# Cook Helper — Design Document

> **版本**: v2.2 | **更新**: 2026-08-04 | **状态**: 一期完成 + 本地化改造完成，二期规划中  
> **v2.2 的变化**：§9 补上 Docker 这一装法（[Task/09](./Task/09-local-web-service-✅已完成.md) ✅），连同「默认不开局域网、因为没有认证」这条边界。  
> **v2.1 的变化**：**§6 决策 #14「hard 约束进代码」已推翻**——过敏与禁忌改由 LLM 判断 + 免责声明，§6 末尾附带日期的纠正块（含推翻它的那份实测证据）。新增 #21：记忆层是第一个需要 API key 的功能。§3 的 B 层职责行同步更新。  
> **v2.0 的变化**：多用户 / 云端形态已废弃，改为**本地单用户 + 纯文本数据**。受影响的 §3 / §4 / §6 / §7 / §8 / §9 / §10 均已重写，其中 §7 与 §9 保留了带日期的战略纠正块。  
> **定位**: 本文档描述「项目是什么、为什么这样设计」——含理念、路径选择与战略结论。技术实现细节见 [SPEC.md](./SPEC.md)；数据格式见 [docs/vault-format.md](./docs/vault-format.md)；路线图与任务拆分见 [FUTURE.md](./FUTURE.md) 与 [Task/](./Task/)。  
> **读者**: 产品经理、设计师、新团队成员、AI Agent（理解项目全貌的第一站）。

---

## 1. 产品概述

### 1.1 一句话

**Cook Helper 是一款厨房管理 Web 应用，帮用户回答三个问题：「家里有什么 → 能做什么菜 → 该买什么」。**

### 1.2 目标用户

**首要用户是作者自己。** 这不是「先找一类用户、再为他们做产品」，而是「作者自己每天需要，顺手开源给同好」。

一期曾以多用户形态上线（自助注册 + RLS 数据隔离），但那是**部署形态**的选择，不是市场定位——**该形态已于 2026-07 废弃**（见 §7 / §9 的战略纠正块），现在是纯本地单用户。真实受众是**自托管 / 开源爱好者**（Home Assistant、Obsidian 那一类人群），不是大众消费者。远期的「本地数据 + 可接其他开源组件」路线注定 onboarding 偏重——这个受众范围是**睁眼选的取舍**，不是遗憾。

### 1.3 产品定位

- **当前（一期）**: 传统 Web 交互——表单 / 列表 / 日历 / 瀑布流，全手动操作。
- **未来（二期）**: 在现有界面**之上**叠一层 AI——自然语言命令层 + 录入折叠器。
- **本质**: AI 是**加速器，不是替代品**。

> ⚠️ **战略纠正（2026-07-25）**
>
> 本文档早期版本写的是「不是给传统界面加对话 bot，而是**用对话取代传统界面**；一期只是二期的可视化后台 + 安全网」。**该定位已废弃。**
>
> **理由**：对话式 UI 不是普遍更优。**浏览**菜谱，瀑布流吊打聊天框；**一眼看状态**（冰箱里有什么），可视化库存吊打问 bot。对话只在**模糊的、多步的、低频的**意图上赢——「帮我排一周的菜，避开最近吃过的，用掉那块快坏的猪肉」。
>
> **因此：一期的 GUI 是永久资产，不是脚手架。** 二期不拆它，只在它之上加命令层。参照 Linear / Raycast / Arc——AI 是命令层，不是替代层。

### 1.4 项目性质与路径选择

本项目明确选择**路径 A：开源个人工具**，不做商业化。

| | **路径 A（本项目）** | 路径 B（不走） |
|---|---|---|
| 成功定义 | 手艺；作者每天真用；一小群人真心喜欢；别人能在此基础上加功能 | 扛得住 AI 竞争的生意；数据资产；规模与留存 |
| 需要护城河吗 | **不需要**——不圈市场 | 必需 |
| 开源 | 核心诉求 | 与数据护城河直接冲突 |
| 日常长什么样 | 打磨到自己用爽 | 客服、留存曲线、追反馈 |

**次级目标**（同等重要，是这个项目存在的一部分理由）：

1. 跑通「开发 → 上线 → 部署」的完整流程
2. 全程以 Vibe Coding 的方式开发，探索这种协作方式的边界
3. 实践 AI-native 产品理念——**先设计 AI 能吃的数据地基，而不是把 AI 硬糊上去**

> 混合模式（开源内核 + 托管服务，如 Supabase / Ghost / Home Assistant 的 Nabu Casa）技术上成立，但对「家庭做饭 App」这个品类，Mealie / Grocy / Tandoor 的历史表明它没有产出过真生意（见 §12）。本项目**不追求这条路**；托管实例 `cook.wreathmoon.com` 仅作演示与便利。

### 1.5 产品哲学与战略结论

以下四条是本项目所有技术决策的上游依据。

#### (1) 护城河归因：耐久性不来自「拥有数据」

数据分两种，护城河性质天差地别：

- **聚合 / 网络数据**（推荐系统、搜索）：价值来自千万用户，对手冷启动极难 → 真护城河，但需要规模。
- **个人数据**（你冰箱里有什么）：价值只对**那一个用户**成立 → 带来的是**留存 / 切换成本**，**不是**竞争护城河。

**每个 App 都能装每个人的个人数据——那就不是谁的护城河。** 真正耐久的三样东西是：**习惯 + system of record（某块生活的系统级记录）+ 品味**。它们规模化不了，恰恰是小体量建造者唯一能赢的地方。

#### (2) 模型进步对本项目永远是顺风

「下一个大模型自带这个功能，把我干掉」——那是**路径 B 的焦虑**（我圈了市场，我怕被抢）。

对路径 A：如果新模型能把「看我冰箱推荐晚饭」做得更好，**换个模型插进 harness，受益的是作者自己。** 这个恐惧在本项目上是伪问题。

技术性事实是：基座模型**没有权限访问你此刻私有的、实时的、结构化的个人状态**。防御性不来自「拥有数据」，而来自「持有 system of record + 围绕它形成的日常习惯」。

#### (3) 唯一失败模式：作者自己弃用

个人工具没有留存曲线要担心，只有一种死法：**作者不再用它。** 头号杀手是**数据变馊**——维护录入是无偿劳动，而这正是人类最讨厌的事。Pantry tracker / meal planner 品类的坟场就是这么堆起来的。

三档库存制（`enough` / `low` / `out`）是对此的缓解，但它仍然要求每次买菜、每顿饭后去点一下。

#### (4) 推论：AI 的首要任务是消灭录入负担

结论 (3) 直接决定二期方向——AI 不是这个产品的威胁，AI 是让它能长期活下去的**唯一办法**，因为只有它能把录入成本压到接近零。优先级：

1. **消灭录入负担**（拍照 → 入库、小票 → 入库、语音 → 「我做完了红烧肉」）：真实、未被开垦，且死穴正在这里。
2. **命令层加速器**（叠在 GUI 之上，专治难查询）：真实但次要。
3. ~~对话取代界面~~：**为了炫而找问题。不做。**

---

## 2. 核心问题与解决思路

| 用户痛点 | Cook Helper 解法 | 对应页面 |
|---------|-----------------|---------|
| "冰箱里有什么？记不住" | 5 分类库存管理，三档制（够/快没了/没了），人和 AI 都好表达 | `/inventory` |
| "买回来的菜忘了吃，坏了" | 清库存推荐——久放食材自动推送含它的菜谱 | `/recommend` |
| "能做什么菜？选择困难" | 两层推荐引擎：先分三档，再档内评分排序 | `/recommend` |
| "该买什么菜？" | 勾选要做的菜 → 自动生成购物清单 → 采购后一键回填库存 | Shopping List |
| "这周吃什么？" | 月视图日历，记录已做 + 规划未来 | `/calendar` |
| "这道菜怎么做？" | 菜谱详情：食材清单、分步步骤、标签、成品照 | `/recipes` |
| "装完是空的，没法用" | 开箱自带 54 道菜谱 + 49 种食材的种子数据，`npm run dev` 打开就有内容 | 首次启动自动初始化 |
| "我的数据在别人手里" | 全部数据是你磁盘上的纯文本文件，可读可改可备份，应用只是透镜 | `data/` 目录 |

---

## 3. 整体架构

```
┌──────────────────────────────────────────────────────┐
│                    用户浏览器                          │
│  ┌──────────────────┐    ┌─────────────────────────┐ │
│  │  Web UI (一期)    │    │  AI 命令层 (二期)         │ │
│  │  ★ 永久资产        │    │  录入折叠 + 命令，不取代UI │ │
│  └────────┬─────────┘    └──────────┬──────────────┘ │
└───────────┼─────────────────────────┼────────────────┘
            │                         │
    ┌───────┴─── Server Actions ──────┴───────┐
    │                                          │
    │  ┌──────────────────────────────────┐   │
    │  │  A 层：Service 纯函数              │   │
    │  │  (一期写，二期 AI Agent 直接调)     │   │
    │  │  inventory / recipe / utensil     │   │
    │  │  calendar / shopping / seed       │   │
    │  └──────────────┬───────────────────┘   │
    │                 │                        │
    │  ┌──────────────┴───────────────────┐   │
    │  │  B 层：推荐决策（一期 TS 规则引擎）  │   │
    │  │  (二期保留为基线，LLM 增强)         │   │
    │  └──────────────────────────────────┘   │
    │                                          │
    └──────────────┬───────────────────────────┘
                   │
    ┌──────────────┴───────────────────────────┐
    │   vault 数据层 (src/lib/vault/)            │
    │  ┌──────────┐ ┌──────────┐ ┌───────────┐ │
    │  │ reader   │ │ writer   │ │ schema    │ │
    │  │ 全量进内存│ │ 原子写   │ │ Zod 校验  │ │
    │  └──────────┘ └──────────┘ └───────────┘ │
    └──────────────┬───────────────────────────┘
                   │
    ┌──────────────┴───────────────────────────┐
    │   本机磁盘：data/kitchen/**  ← 唯一真相      │
    │   *.md（菜谱） *.yaml（库存/厨具/日历/配置） │
    │   照片就放在菜谱目录里                       │
    └──────────────────────────────────────────┘
```

> 没有数据库、没有后端服务、没有账号系统。整个应用是一个**跑在你自己机器上的本地 Web 服务**，
> 数据是它旁边的一堆纯文本文件。

### 架构分层职责

| 层 | 职责 | 一期（已建） | 二期（规划） |
|----|------|:-----------:|:----------:|
| **UI 层** | 展示与交互 | Ant Design 传统控件 | **GUI 全部保留** + 叠加命令层与录入入口 |
| **Server Actions** | 前后端桥梁 | `src/app/actions/*.ts` | 不变，复用 |
| **A 层 Service** | 数据增删改执行 | 纯函数，同构可复用 | **AI Agent 调同一批函数** |
| **B 层 Recommend** | 推荐决策 | TypeScript 规则引擎 | **保留为基线**（库存分档、清库存、不重样等照旧无需 API key）；**记忆类偏好由 LLM 判断**，见 §6 #14 纠正块 |
| **数据层** | 持久化 | **本地 vault 纯文本文件**（`src/lib/vault/`），启动时全量读进内存 | 不变；远期加派生索引（仅当实测不够快时） |

> **核心原则**: 一期业务逻辑不写死在 React 组件里，全部下沉到 `src/lib/services` 纯函数。二期 AI Agent 通过 function calling 直接调这批函数。

---

## 4. 页面功能地图

**没有登录，也就没有「公开 / 需登录」之分**——只跑在本机，所有页面一律可达。

```
/                    → 直接重定向到 /recommend
│
├── /recommend       ★ 首页 — 智能推荐 + 筛选 + 购物清单闭环
├── /inventory       5 分类（蔬菜/肉类/蛋奶豆/主食/调料）三档库存 + CRUD
├── /utensils        厨具清单（名称 + 分类 + 备注）
├── /recipes         菜谱 CRUD + 多维标签筛选 + 食材关联 + 成品照
│   └── /recipes/new 新建菜谱
├── /calendar        月视图日历 + 烹饪记录/规划 + "我做完了"库存更新
└── /api/photo       读取 vault 里的照片文件（含路径越界防护）
```

> **只读沙盒**：同一份代码加 `READ_ONLY=1` 部署，就是一个可公开访问的演示实例——
> 读仓库里的 `seed/` 展示，所有写入优雅拒绝并引导去本地部署。它顶替了原来的 `/demo` 页。

### 各页面核心功能

| 页面 | 核心能力 |
|------|---------|
| `/recommend` | 三档推荐卡片（现在就能做 ✅ / 需购买 🛒 / 清库存 ⏰）+ 时间/辣度/荤素筛选器 + 勾菜→出清单→回填 |
| `/inventory` | 5 分类 Tab + 搜索 + CRUD + 三档库存颜色标识（绿=够 / 橙=少 / 红=没） + 批量更新库存 |
| `/utensils` | 极简列表 + 搜索 + 添加/删除 |
| `/recipes` | 瀑布流卡片 + 多维标签筛选 + 详情弹窗（含食材状态绿/黄/红）+ 成品照上传（浏览器内先压缩再落盘） |
| `/calendar` | Ant Design Calendar 月视图 + 每日菜名标签 + 点击日期展开详情 + "我做完了"弹窗调库存 + 照片上传 |
| 空态 | 每个列表在无数据时给「图标 + 为什么要填 + 单一主行动」，而不是一句「暂无数据」 |

---

## 5. 核心数据流（推荐闭环）

```
库存变化（用户手动改 / 采购回填 / 做完菜更新）
         ↓
推荐重新计算（tiering 分档 → scoring 排序）
         ↓
三档卡片展示
  ├─ can_make_now   (食材全齐)      → 🟢 现在就能做
  ├─ need_shopping  (缺 ≤3 样)      → 🟡 还需采购
  └─ clear_stock    (久放食材)       → 🟠 清库存推荐
         ↓
用户勾选想做的菜 → 生成购物清单
  = 缺的食材 + low/out 的调料/主食 + 可选：计划菜的缺失
         ↓
采购后清单打钩 → 库存自动回填 enough → 回到第一步
```

---

## 6. 关键设计决策

| # | 决策 | 理由 |
|---|------|------|
| 1 | **库存三档制** (enough/low/out) 而非精确数量 | 精确数量人和 AI 都难维护；三档制表达自然，是 AI 能成立的数据地基 |
| 2 | **Recipe attributes 用 jsonb** 而非独立列 | 新标签维度零迁移添加；二期 AI 读写半结构化数据也顺手；GIN 索引保查询性能 |
| 3 | **A/B 层分离** | A 层（执行动作）二期复用；B 层（推荐决策）二期**保留为基线**并被 LLM 增强——见 #14 |
| 4 | **一期纯规则推荐** | 轻量验证需求；且规则引擎无 API key 依赖——二期作为「没有 key 也完全能用」的基线保留 |
| 5 | **Server Actions 而非 REST API** | 与 Next.js App Router 深度集成；类型安全；减少样板代码；同一批函数既供 UI 也供 Agent |
| 6 | **不做独立 Dashboard** | 打开直接进 `/recommend`（核心价值页）；不做空壳仪表盘；移除 react-grid-layout |
| 7 | **文案集中在 text.ts** | 成本等同硬编码字符串；但二期接 next-intl 只需改一处 |
| 8 | ~~种子数据配套复制 + 外键重映射~~ → **首次启动复制 `seed/` 目录** | 没有注册流程了。种子是一份调好档位的 vault，复制目录即可，最易错的外键重映射环节整体消失 |
| 9 | ~~Demo 纯前端 fixture~~ → **`READ_ONLY=1` 只读沙盒** | 同一份代码 + 一个开关，不再维护第二套假数据；写入优雅拒绝 |
| 10 | ~~RLS `user_id = auth.uid()`~~ → **无认证，只跑本机** | 单用户本地部署下没有「别人的数据」可隔离；要远程访问交给反向代理（见 §7）|
| 11 | **一期不碰 LLM** | 无 API key 依赖；所有功能手动可用；二期再引入 LLM 替换 B 层 + 新增 `/chat` |
| 12 | **清库存按分类设不同阈值** | 蔬菜 3 天、肉类 7 天、蛋奶 5 天、主食/调料不提醒——贴近真实食材保质期差异 |
| 13 | **AI 定位为命令层，不取代 GUI** | 浏览与看状态是 GUI 强项；对话只在模糊·多步·低频意图上赢。GUI 是永久资产（§1.3） |
| 14 | ~~hard 约束进代码，soft 偏好进 prompt/评分~~ → **记忆一律由 LLM 判断 + 免责声明** | 代码过滤依赖完整的配料数据，而这份数据不存在（见下方纠正块）。改为模型筛查 + 明示不保证准确 |
| 15 | **关联键用「名称」而非 UUID 外键** ✅ 已落地 | 数据存纯文本文件，UUID 对人不可读、手改文件时无法维护。配套一张别名表（「番茄 = 西红柿」）做归一化 |
| 16 | **文件为唯一真相 + 全量读进内存** | 49 食材 + 54 菜谱的量级，启动时全量解析是毫秒级，`Map.get(name)` 就是 O(1)。**不引 SQLite**——native addon 的跨平台编译问题会直接破坏「clone 完就能跑」 |
| 18 | **归一化在数据层做，推荐引擎不碰** | 让 `InventoryItem.id` 就是归一化后的名称，推荐引擎照旧按 id 查表、源码一行不改。归一化本就是数据层职责，混进推荐层只会让核心逻辑变脆 |
| 19 | **手改坏了文件必须报到「哪个文件第几行」** | 这是「你可以拿记事本改」这个承诺的兑现处。报错说不清位置，等于把承诺收回去了 |
| 20 | **写入一律 temp + rename** | 写到一半断电／被强杀时，文件要么是旧内容要么是新内容，不会半截。单用户场景不加锁 |
| 17 | **暴露给 Agent 的数据 opt-in** | 不是所有数据都该进 LLM 上下文；远期接入理财等模块时「默认不暴露」是正确默认值，也省 token |
| 21 | **记忆层是第一个需要 API key 的功能** | #14 改为 LLM 判断后的直接后果。#4 / #11 的「无 key 也完全能用」**依然成立但需限定范围**：指的是一期全部功能（库存、菜谱、日历、规则推荐），**不含记忆层**。没配 key 时记忆能写能看，但不影响推荐——这个状态必须在 UI 上明示，不能让用户以为它在生效 |

> ⚠️ **战略纠正（2026-08-04）：决策 #14 已推翻**
>
> 本表 #14 原文是：「**hard 约束进代码，soft 偏好进 prompt/评分** —— 提示词遵从性不是 100%；过敏与医疗禁忌必须在 `tiering.ts` 里过滤掉，让模型根本看不到」。**该方案已废弃。**
>
> **理由**：这条决策的前提是「代码能判断一道菜含不含某种成分」。核实种子数据后发现前提不成立——
> `seed/kitchen/recipes/宫保鸡丁/recipe.md` 的食材表是「鸡胸肉、青椒、胡萝卜、花椒、干辣椒、葱姜蒜、生抽、醋、白糖、淀粉」，
> **没有花生**；「花生」在整个 `inventory/` 里也不存在。而这不是种子数据没写好，是结构性的：
> 食材表记的是「冰箱里有什么」，服务于库存与采购，没人会把一勺花生油录进冰箱。
>
> **确定性的代码 + 残缺的数据 = 确定性地给出错误答案**，而且比模型出错更危险——它看起来很可靠。
> 一个漏掉宫保鸡丁的花生过敏过滤，比没有过滤更有害，因为用户会依赖它。
>
> **因此**：过敏与禁忌作为记忆字段默认存在，但**判断交给 LLM**（[Task/10](./Task/10-memory-layer.md) → [Task/12](./Task/12-ai-command-layer.md)），
> 并在设置页明示**不保证准确、请用户自行核对成分**，口径参照大模型「不提供医疗建议」的免责说明。
>
> **仍然成立的那半句**：**提示词遵从性不是 100%。** 区别在于，现在这一点是**对用户明示的已知限制**，而不是一个假装被代码解决掉的问题。

---

## 7. 安全模型

> ⚠️ **战略纠正（2026-07-26）**
>
> 本节原先描述的是四层模型：「Middleware 路由守卫 → RLS 行级安全（`user_id = auth.uid()`）→ Storage RLS → Service Role 特权」。**该模型已随多用户形态一并废弃。**
>
> **理由**：它保护的是「用户 A 读不到用户 B 的数据」，而单用户本地部署下**根本不存在用户 B**。为一个不存在的威胁维护认证 + RLS + 多租户 + 特权逻辑，成本全部由作者独自承担（见 §1.2 / §1.4）。
>
> **因此**：现在的安全模型是下面这一条。

**没有认证。应用默认只监听 `localhost`，安全边界是「这台机器」。**

| 关注点 | 现状 |
|--------|------|
| 谁能访问 | 只有能操作这台机器的人。默认不监听 `0.0.0.0`，局域网内其它设备访问不到 |
| 数据在哪 | `data/` 目录，权限就是文件系统权限 |
| 远程访问 | **交给反向代理**（Tailscale / Cloudflare Access / 自建 nginx + 认证），不由应用负责 |
| 只读实例 | `READ_ONLY=1` 时所有写入在服务端被拒绝（`assertWritable()`），客户端的拦截只是 UX |
| 照片读取 | `/api/photo` 只允许读 vault 目录内的图片文件，`..` 越界路径直接 403 |

> ⚠️ **不做「保留一个登录以防万一」。** 半个认证系统比没有认证更危险——它会让人以为数据被保护着。
> 要么没有认证 + 明确说明只跑 localhost，要么交给成熟的反向代理。

---

## 8. 数据模型概要

每个实体对应一个（或一组）纯文本文件，**没有表、没有外键、没有 join**——关联一律用**名称**。

| 实体 | 文件 | 关键特性 |
|------|------|---------|
| 食材/调料 | `kitchen/inventory/{分类}.yaml`（5 个） | 三档库存；`last_restocked_at`（清库存推荐来源）；`price` 可选参考价；按名称排序，diff 干净 |
| 厨具 | `kitchen/utensils.yaml` | name + 分类 + 备注 |
| 菜谱 | `kitchen/recipes/{菜名}/recipe.md` | YAML frontmatter（属性/食材/厨具）+ Markdown 正文（步骤/Tips） |
| 菜谱照片 | 与 `recipe.md` **同目录** | 整个目录可以直接发给别人 |
| 烹饪记录 | `kitchen/calendar/{YYYY}-{MM}.yaml` | 按月分片，改本月不碰上个月，同步时少冲突 |
| 食材别名 | `kitchen/aliases.yaml` | 「番茄 = 西红柿」。名称做关联键的前提 |
| 推荐配置 | `kitchen/config.yaml` | 权重与阈值外置，改完重启生效，不用改代码 |

三种数据形状对应三种存法：**文档型**（菜谱）用 Markdown + frontmatter；**记录型**（库存/厨具/日历）用按名称排序的 YAML 数组；**二进制**（照片）用独立文件 + 相对路径引用。

> 完整字段定义见 [docs/vault-format.md](./docs/vault-format.md)，实现细节见 [SPEC.md](./SPEC.md)。

---

## 9. 部署架构

**主形态：跑在你自己机器上。**

```
你的浏览器  →  http://localhost:7474
                     │
              Next.js（本机进程）
                     │
              data/kitchen/**（本机文件）
```

```bash
git clone … && npm install && npm run dev    # 就这三行，没有第四步
```

**同一形态的另一种装法：Docker。** 给不想在机器上装 Node 的人——一条 `docker compose up --build`，
数据仍然是你目录里那堆文件（容器挂的就是同一个 `data/`），两种跑法可以随时互换。
自托管圈子（Home Assistant 那一挂）的默认交付形态就是它。

> 端口**默认只对本机开放**。开给局域网是一行的事，但这个应用没有任何认证——
> 谁能连上谁就能读写你的全部数据，所以默认不开，且开关旁边就写着这句话。

**附带形态：只读演示实例。**

```
访客浏览器  →  https://cook.wreathmoon.com
                     │
              Vercel（同一份代码 + READ_ONLY=1）
                     │
              仓库里的 seed/（只读，重启即重置）
```

Vercel 的文件系统本来就是只读的，所以这个形态几乎是白拿的：读种子数据展示，
写入优雅报错并引导去本地部署。**不可注册、不保存任何访客数据。**

> ⚠️ **战略纠正（2026-07-26）**：本节原先描述的是「Vercel + Supabase + Resend 三件套，
> 自助注册 + RLS 数据隔离」的云端多用户部署。**该形态已废弃**，理由见 §7 的纠正块。

---

## 10. 技术栈一览

| 层 | 选型 | 备注 |
|----|------|------|
| 框架 | Next.js 16 (App Router) + React 19 | |
| UI | Ant Design 5 + ProComponents | 基于 Design Token，支持 light/dark |
| 语言 | TypeScript | |
| 状态 | Zustand | 仅 UI 状态（主题、侧边栏），不缓存服务端数据 |
| 数据存储 | **纯文本文件**（YAML + Markdown） | 本机 `data/` 目录，无数据库 |
| 解析 | `yaml` | 唯一为数据层新增的运行时依赖 |
| 校验 | `zod` | vault schema 校验，产出指向文件+行号的报错 |
| 认证 | **无** | 只跑本机；远程访问交给反向代理（§7）|
| 照片 | 本机文件，存在菜谱目录里 | 浏览器内先压缩（1600px / JPEG 0.82）再落盘 |
| 测试 | Vitest | 97 个单测，覆盖推荐引擎、vault 数据层、A 层服务、种子质量 |
| 部署 | 本机 `npm run dev`；只读沙盒在 Vercel | 见 §9 |
| 域名 | cook.wreathmoon.com | 仅承载只读演示实例 |

---

## 11. 开发分期

| 阶段 | 范围 | 状态 |
|------|------|:----:|
| **一期 MVP** | 传统 Web UI：5 大业务模块 + 规则推荐 + 购物清单 + 主题切换 | ✅ 完成 |
| **本地化改造** | 去多用户 / 去云端：数据改为本机纯文本 vault，开箱自带种子数据，只读沙盒顶替 Demo 页 | ✅ 完成 |
| **一期之后的全部分期** | 见 [FUTURE.md](./FUTURE.md) | 📋 |

> ⚠️ **单一信源**：为避免 DESIGN / SPEC / PRD 三处各说一套分期，**一期之后的全部路线图、架构演进与任务拆分统一由 [FUTURE.md](./FUTURE.md) 承载**，本表不再维护。**不要**在本文档里另起一份路线图——那会造成两处漂移。
>
> 特别注意：早期文档里的「二期 = 新建 `/chat` 路由 + LLM 整体替换 B 层」已按 §1.3 的战略纠正废弃——二期不建独立聊天页取代界面，改为「**录入折叠 + 命令层**」两条腿。

---

## 12. 竞品与范本对照

### 12.1 同品类开源竞品（一期并不新颖，这是有用的信息）

| 项目 | 覆盖范围 | 对本项目的意义 |
|------|---------|--------------|
| **Mealie** | 菜谱管理 + meal plan + 购物清单，自部署 | 几乎是一期的直接对照物 |
| **Grocy** | 「家庭 ERP」：库存 + 采购 + 保质期 | 离「有什么 → 买什么」这个论点最近 |
| **Tandoor** | 菜谱 + 计划 + 购物清单 | 同上 |
| **Cooklang** | 菜谱的纯文本格式标准 | 纯文本菜谱格式的先例，定 vault 格式时值得参考 |
| **Paprika** | 菜谱管理（闭源商业） | 单机菜谱工具的体验基线 |

**它们的共同天花板**：录入负担极重、无 AI、界面糙、只有 power user 玩得动。而且**没有一个跑出真正的商业化**——这不是巧合，见 §1.4。

**结论**：一期做的事已有成熟开源实现，所以力气不该投在「再做一个功能更全的 CRUD」，而该投在 **品味 + 消灭录入负担**（§1.5 (4)）。

### 12.2 未来模块的现成对照（不重造轮子）

远期若扩展到其他生活域，每一块都已有成熟开源实现，应优先考虑**借用 / 对接**而非重写：

| 域 | 现成项目 |
|----|---------|
| 记账 / 资产 | **Firefly III** |
| 人物 / 生日 / 人际 | **Monica**（personal CRM） |
| 订阅追踪 | **Wallos** |

### 12.3 两个架构范本

#### Obsidian —— 给灵魂，不给管道

- **借鉴**：local-first、用户拥有自己的数据、App 只是「透镜」而非「牢笼」、纯文本、插件生态。
- ⚠️ **不能照搬**：Obsidian 的插件模型之所以干净，是因为底层**同质**——整个 vault 全是 markdown，一种存储一种格式。而个人生活数据是**异质**的（各工具各有 schema / API / 鉴权），难一个量级。
- ⚠️ **要避开的软肋**：Obsidian 的链接靠**文件名**，改名即断。本项目改用「frontmatter 里的稳定 ID」（§6 #15）。

#### Home Assistant —— 给管道

> 注：是 **Home Assistant**（开源智能家居中枢，跑在自己的树莓派 / NAS 上），**不是** HarmonyOS 鸿蒙。

它解决的正是本项目远期的同构问题：面对上千种互不兼容的外部系统，统一成能被自动化和语音调度的东西。7 条借鉴点：

| # | 借鉴点 | 对应到本项目 |
|---|-------|------------|
| 1 | **统一 entity 抽象**：一切皆 `state` + `attributes` | 三档 `stock_level` 就是极好的 `state`，`attributes` jsonb 就是 `attributes`——已具雏形，需提升为全系统契约 |
| 2 | **统一动作命名空间**（`light.turn_on`） | `kitchen.set_stock_level` 等；自描述的 service 定义可直接生成 agent 的 tool schema |
| 3 | **连接器与大脑分离**（integration vs automation） | 即现有 A 层 / B 层分离——扩展时别搞混 |
| 4 | **opt-in 暴露给 AI**（逐个 entity 授权） | §6 #17 |
| 5 | **派生历史库可丢弃**（recorder） | §6 #16 |
| 6 | **本地优先，云只卖便利**（Nabu Casa） | 不背叛开源前提下的可选托管形态 |
| 7 | **blueprint：分享行为，不只分享数据** | 社区不只分享菜谱，也可分享推荐配置 / prompt / 食材别名表 |

> **检验扩展架构是否正确的唯一标准：加第 2 个模块的成本 ≈ 第 1 个的 1/5。** 若加第二个模块依然很痛，说明在盖单体巨石，应停下重构。

---

## 13. 反模式清单（明确不做什么）

| # | 不做 | 为什么 |
|---|------|-------|
| 1 | **对话取代 GUI** | §1.3——为了炫而找问题 |
| 2 | **mega-dashboard**（一屏看尽所有生活域） | 「读」的价值很弱：几乎从不需要同屏看冰箱和净资产。真正的价值在**写入侧**（一次输入扇出到多个系统）。统一视图应由 agent 按需拼出，不预制面板 |
| 3 | **记忆检索上向量库** | 个人记忆是几十到几百条量级，关键词 + scope 过滤完胜 embedding 且可调试。典型过度工程 |
| 4 | **单体巨石**（所有域塞进一套 schema） | 记账要复式严谨、人脉要关系图、厨房要库存 + 菜谱；硬塞的结果是 5 个浅薄 App 而不是 1 个好的，然后单人淹死在维护里 |
| 5 | **追求大众受众** | 本地 / 联邦路线的 onboarding 天然偏重，受众是自托管爱好者——睁眼选的取舍（§1.2） |
| 6 | **为了护城河做设计** | 路径 A 不圈市场，不需要护城河（§1.4） |
| 7 | **优先做「最上镜」的模块** | 例如「合成穿衣照」：AI 卖点是纯模型能力、跨域数据协同薄——最上镜却最不能验证架构 |

---

> **本文档随架构变化更新。技术实现细节见 [SPEC.md](./SPEC.md)；未来规划与任务拆分见 [FUTURE.md](./FUTURE.md)。**
